package defpackage;

import android.content.Context;
import android.net.Uri;
import android.os.ParcelFileDescriptor;

public final class it implements ij<Integer, ParcelFileDescriptor> {
    public final ii<Integer, ParcelFileDescriptor> a(Context context, hs hsVar) {
        return new is(context, hsVar.a(Uri.class, ParcelFileDescriptor.class));
    }
}
